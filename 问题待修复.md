# 问题待修复

> 📊 **状态总览**（截至 v2.7.2）
>
> | # | 问题 | 状态 | 修复版本 |
> |---|---|:---:|:---|
> | 1 | 请求次数和 token 使用量分开统计，切换时 bug | ✅ 已解决 | v2.3bate |
> | 2 | 支持用户使用 Anthropic 格式调用 | ✅ 已解决 | v2.3bate |
> | 3 | 用户 API 密钥（api_key）未到期自动刷新 | ✅ 已解决 | v2.3bate |
> | 4 | hfadmin无法拖动模型池内模型或子池顺序 | ✅ 已解决 | v2.3bate |
> | 5 | 模型管理中无法调换顺序，或调换后刷新恢复原状 | ✅ 已解决 | v2.3bate + v2.3.2 |
> | 6 | v1/chat/completions 返回 content 为空 | ✅ 已完成 | 诊断确认为上游/隧道侧，网关日志备查 |
> | 7 | 模型管理界面无法拖拽供应商及供应商内模型顺序 | ✅ 已完成 | — |
> | 8 | 模型池内拖动排序时界面闪烁 | ✅ 已完成 | — |
> | 9 | 拖动模型池时页面重新加载且不支持拖拽自动滚动 | ✅ 已完成 | — |
> | 10 | hfadmin 点击编辑/添加等按钮时页面闪烁重载 | ✅ 已完成 | — |
> | 11 | 父池调用时子池 json_output 标记未透传 | ✅ 已完成 | v2.7.2 |
> | 12 | embedding 模型在主界面显示为纯文本 | ✅ 已完成 | v2.7.2 |
> | 13 | 测试连通性对嵌入模型发 chat/completions 请求致 400 | ✅ 已完成 | v2.7.2 |
> | 14 | auto 池内无法拖拽移动池内模型和子池顺序 | ⚠️ 待修复 | 用户实测确认无法拖拽 |
> | 15 | 均衡负载 (load_balance) 逻辑疑问：怀疑未实际启用 | ✅ 已确认生效 | v2.7.2 实测轮转+文档说明 |
> | 16 | 模型编辑页未根据计费类型条件隐藏字段 | ⚠️ 待修复 | — |
> | 17 | 拖拽池和模型时强制刷新整页且多次提示"已改变顺序" | ⚠️ 待修复 | — |
> | 18 | token 用量计算准确度不足 | ⚠️ 待优化 | — |
> | 19 | 模型达到限额后仍被路由尝试（先被拦截再轮转下一个） | ⚠️ 待修复 | — |
> | 20 | auto_order 与 load_balance 前端未互斥，可同时开启 | ⚠️ 待修复 | — |
>
> **说明**：问题 1-13 已解决或确认；14 经用户实测确认复现；16-20 于 2026-09-01 用户反馈新增。
>
> **说明**：问题 1-10 已全部解决（1-5 随 v2.3bate/v2.3.2 发布；6 经诊断确认为上游/隧道侧；7-10 于 v2.7.1 确认完成）。当前待处理：问题 11-15（修复计划见文末）。问题 11-13 于 2026-08-30 配置 Mem0 外部记忆插件时发现/反馈。问题 14-15 于 2026-08-30 用户实测反馈新增。

---

## 1. ✅ 已解决 (v2.3bate) — 请求次数和 token 使用量分开统计，切换时 bug

### 问题描述
当前统计面板中，切换统计维度时，token 使用量（如 1200 tokens）会被错误地展示为调用次数（1200 次调用）。虽然此功能不常用，但属于明显的展示 bug，需要修复。

### 复现步骤
1. 打开统计面板，默认显示调用次数统计。
2. 切换到 token 使用量统计维度。
3. 观察展示数据：token 使用量（如 1200 tokens）被错误地展示为调用次数（1200 次调用）。
4. 切换回调用次数统计，数据展示正常。

### 期望行为
- 切换统计维度时，应正确展示对应维度的数据。
- 调用次数统计维度展示各时间段的 API 调用次数。
- Token 使用量统计维度展示各时间段的 token 消耗总量。
- 切换过程中数据应正确刷新，不出现数据错位。

### 影响范围
- 统计面板页面
- 用户查看统计数据时的体验
- 若用户依赖 token 使用量数据进行成本分析，会导致错误结论

## 2. ✅ 已解决 (v2.3bate) — 支持用户使用 Anthropic 格式调用

### 问题描述
当前网关仅支持 OpenAI 格式的 API 调用。需要升级，支持用户用 Anthropic 格式（messages 中的 role 为 "assistant"/"user" 等，content 为 list 格式）发送请求。需要做格式转换，将 Anthropic 格式转为 OpenAI 格式再转发给后端。

### 复现步骤
1. 用户以 Anthropic 格式发送请求（如 role 为 "assistant"/"user" 的 messages，content 为 list 格式）。
2. 网关当前无法识别该格式，请求失败或返回错误。
3. 使用 OpenAI 格式发送相同请求，则正常处理。

### 期望行为
- 网关能正确识别并处理 Anthropic 格式的请求。
- 自动将 Anthropic 格式转换为 OpenAI 格式，再转发给后端模型。
- 转换后的请求能正确得到响应，并将响应以合适格式返回给用户。
- 不破坏现有 OpenAI 格式的支持。

### 影响范围
- 网关 API 入口层
- 所有通过网关调用后端模型的用户
- 使用 Anthropic 客户端或 SDK 的用户
- 网关的格式转换模块（新增）

## 3. ✅ 已解决 (v2.3bate) — 用户 API 密钥（api_key）未到期自动刷新

### 问题描述
用户在 dashboard 设置 API 密钥过期时间（如 5 小时或 1 天）后，到期时密钥不会自动刷新，而是保持不变。用户需要手动再次修改密钥才能重新启用，导致过期设置形同虚设。

### 复现步骤
1. 管理员为用户设置 API 密钥，并指定过期时间（例如 5 小时或 1 天后过期）。
2. 等待过期时间到达。
3. 使用已过期密钥发起 API 请求。
4. 观察到密钥状态：仍保持原密钥值不变，未自动更换为新密钥。
5. 使用过期密钥的 API 请求可能被接受（若未做过期校验）或返回固定错误（若做了校验但未刷新），用户无法继续使用服务。

### 期望行为
- **方案 A（推荐）**：到期自动刷新 —— 过期时间到达后，系统自动为用户生成新密钥并更新，旧密钥同时失效，用户无感切换。
- **方案 B（备选）**：到期后保留原密钥并做校验 —— 请求时校验密钥是否过期，过期则返回 401 并提示用户重新设置，同时日志记录该事件。

无论哪种方案，需确保：
- 过期设置生效，不形同虚设。
- 用户/管理员能知晓密钥状态（已过期 / 已刷新）。
- 过期时间、刷新时间的日志可追溯。

### 影响范围
- API 密钥管理模块（密钥存储、校验、刷新逻辑）
- 用户认证与安全（到期密钥是否仍可被使用）
- 管理后台的密钥设置界面
- 安全审计日志

## 4. ✅ 已解决 (v2.3bate) — hfadmin无法拖动模型池内模型或子池顺序（**/admin 同样存在**）

### 问题描述
在 /hfadmin 后台中，模型池内的模型或子池无法通过拖动来调整顺序。用户尝试拖动但无效果，顺序保持不变。

**`/admin`（原版面板）同样存在此问题**：原面板在 2.1bate 加入 hfadmin 后未改动（commit `31da20b` "原面板零改动"），其前端从未实现拖拽事件，模型池内模型/子池列表根本没有拖拽手柄，无法调换顺序。

### 复现步骤
**`/hfadmin` 复现：**
1. 登录 hfadmin 后台
2. 进入模型池页面
3. 尝试拖动模型池内的模型或子池，调整排列顺序
4. 观察到顺序未发生变化

**`/admin`（原版）复现：**
1. 登录 /admin 后台
2. 进入模型池页面
3. 模型池内模型/子池列表**根本没有拖拽手柄**，无法拖动
4. /admin 自 2.1bate 加入 hfadmin 后零改动（commit `31da20b`），其前端从未实现拖拽事件

### 期望行为
- /hfadmin：支持拖动调整模型池内模型或子池的排列顺序；拖动后顺序持久化保存，刷新页面后仍保持新顺序
- /admin：补齐与 /hfadmin 同等的拖拽功能（共用同一套 `/admin/*` API）

### 影响范围
- /hfadmin 后台模型池管理页面（明文记录）
- /admin 后台模型池管理页面（原面板零改动，前端未实现拖拽事件）
- 前端拖拽交互逻辑（**双前端都需补**：static/hfadmin.html + static/index.html）
- 后端排序持久化接口

## 5. ✅ 已解决 (v2.3bate + v2.3.2) — 模型管理中无法调换顺序，或调换后刷新恢复原状（**/admin 同样存在**）

### 问题描述
在 /hfadmin 后台的模型管理页面中，无法调换模型顺序。即使短暂调换成功，刷新页面后顺序又恢复原状，说明排序未持久化。

**`/admin`（原版面板）同样存在此问题**：原面板在 2.1bate 加入 hfadmin 后未改动（commit `31da20b` "原面板零改动"），其前端从未实现模型顺序调换控件。后端 `/admin/models/reorder` 端点完全缺失（`admin.py` 中只有 `/admin/providers/reorder` 与 `/admin/pools/reorder`，没有 models 的），因此即使前端做对，顺序也无法持久化。

### 复现步骤
**`/hfadmin` 复现：**
1. 登录 hfadmin 后台
2. 进入模型管理页面
3. 尝试调换模型顺序
4. 刷新页面
5. 观察到顺序恢复为调换前的原状

**`/admin`（原版）复现：**
1. 登录 /admin 后台
2. 进入模型管理页面
3. 模型列表**没有可拖拽/可调换顺序的控件**
4. /admin 自 2.1bate 加入 hfadmin 后零改动（commit `31da20b`）

### 期望行为
- /hfadmin：支持在模型管理页面调换模型顺序；调换后顺序持久化保存，刷新页面后仍保持新顺序
- /admin：补齐与 /hfadmin 同等的排序功能（共用 `/admin/*` API）
- 后端补充 `/admin/models/reorder` 端点（当前缺失）

### 影响范围
- /hfadmin 后台模型管理页面（明文记录）
- /admin 后台模型管理页面（原面板零改动）
- 前端拖拽/排序交互逻辑（**双前端都需补**：static/hfadmin.html + static/index.html）
- 后端排序持久化接口（**`/admin/models/reorder` 端点缺失**，admin.py 中只有 providers/pools reorder）

## 6. ✅ 已完成 — v1/chat/completions 返回 content 为空

### 问题描述
通过 Sakura 隧道或内网调用 `v1/chat/completions` 接口时，返回的 assistant message `content` 字段始终为空字符串 `""`，但 `completion_tokens` 和 `total_tokens` 计数正常，`finish_reason` 为 `length`。

### 复现步骤
1. 调用 `POST /v1/chat/completions`，header `x-api-key: 200202`
2. 任意模型池（`auto` / `GLM-FREE全家桶` 等）均复现
3. 返回 JSON 中 `choices[0].message.content` 为空

复现代码：
```bash
curl -s "http://127.0.0.1:8650/v1/chat/completions" \
  -H "x-api-key: 200202" \
  -d '{"model":"auto","messages":[{"role":"user","content":"hi"}],"max_tokens":500}'
```

返回示例：
```json
{"choices":[{"message":{"content":"","tool_calls":null},"finish_reason":"length"}],"usage":{"prompt_tokens":11,"completion_tokens":500,"total_tokens":511}}
```

### 期望行为
- `content` 字段应包含模型的实际回复内容
- 不应为空

### 影响范围
- `v1/chat/completions` 响应拼装逻辑（main.py 中 `_wrap_key_stream` 或相关响应构建）
- 所有通过隧道或内网调用网关的外部用户
- 疑似与 provider 返回格式解析或 content 字段提取有关

### 实际修复预案（原修复计划.md Phase 5，待诊断日志结论后执行）
根据已部署的 DEBUG/WARNING 日志运行后的结论，按以下三种情况处置：
- **情况 A**：上游原始 content 就为空 → 不是网关 bug，仅保留 WARNING 日志，告知用户检查上游模型/Sakura 隧道
- **情况 B**：网关丢失 content → 检查 `main.py` 响应返回路径或 `format_adapter.py` 转换路径并修复
- **情况 C**：上游与网关日志都正常 → 问题在 Sakura 隧道或客户端解析层，网关侧无能为力

---

## 修复计划（2026-08-20 整理）

### 范围规则
- 后端修复 → 双前端自动生效（`/admin` 与 `/hfadmin` 共用同一套 `/admin/*` API）
- 前端修复 → 必须**同时改** `static/index.html` 和 `static/hfadmin.html`

### 阶段划分

**Phase 0（10 min）双前端差异扫描**
- 列出 `/admin` 与 `/hfadmin` 在功能、DOM 结构、前端框架上的差异
- 避免修 hfadmin 时漏掉 admin 的同名 bug

**Phase 1（15 min）补 `/admin/models/reorder` 端点（修问题 5）**
- 根因（高置信度）：`admin.py` 完全缺少该路由，hfadmin/model 顺序无持久化路径
- 改动：仿 `reorder_providers` (admin.py:284) 模式，新增 `PUT /admin/models/reorder`
- 验证：curl 测试 + 重启后 hfadmin 拖拽 → 顺序保持

**Phase 2（20 min）诊断日志定位问题 6**
- 上游解析 (`openai_provider.py:87`) 与响应拼装 (`main.py:178`) 均不会清空 content → 优先排除"上游/Sakura 隧道"侧
- 改动：在 `openai_provider.py` 收包后 + `main.py` 返回前各加一条 DEBUG 日志，打印原始/最终响应
- 验证：跑用户复现命令，区分三种情况（上游未生成 / 网关丢失 / 隧道截断）

**Phase 3（45-90 min）双前端拖拽修复（修问题 4）**
- 排查 `static/hfadmin.html` 与 `static/index.html` 的拖拽事件
- 工作量约 1.5 倍：先核对两者用同一拖拽库（Sortable.js / 原生 HTML5 / 其他），再分别修
- 验证：两套面板均手动 e2e 测试

**Phase 4（1-2 h）P3 历史问题体检（修问题 1/2/3）**
- 4.1 统计面板切换：双前端 JS
- 4.2 Anthropic 格式：`curl /v1/messages` 验证（README 声称支持）
- 4.3 Key 自动轮换：创建 `expire_seconds=10` 的测试 key 验证 `maybe_rotate_expired`

**Phase 5（待 Phase 2 结论）P0·6 实际修复**
- A. 上游 model 异常 → 加 WARNING 日志 + 可选 fallback
- B. 网关拼装 bug → 修 `main.py` / `format_adapter.py`
- C. Sakura 隧道截断 → 网关无法修复，建议用户换隧道/直连验证

### 优先级与依赖
推荐顺序：**Phase 1 → Phase 2 → Phase 0 → Phase 3 → Phase 4 → Phase 5**

- Phase 1（高 ROI 零风险）→ 最先
- Phase 2（不修代码只加日志）→ 与 Phase 1 并行安全
- Phase 0/3/4/5（依赖前面结论）

### 风险与未知

| 项 | 说明 | 缓解 |
|---|---|---|
| hfadmin.html 1325 行单文件 | 读懂前端结构耗时 | grep `Sortable\|draggable\|dragstart` 定位 |
| index.html 也是大单文件 | 可能用了不同前端框架 | 读 100 行 head/body 段先确认 |
| config.json 是占位符 | 实际运行配置位置未知 | 需用户确认生产 config 路径 |
| Sakura 隧道是第三方 | 网关无法控制 | 排除网关侧后让用户换隧道/直连 |
| admin 是遗留版本 | 可能还有 hfadmin 没有的隐藏 bug | Phase 0 差异扫描时留意 |

## 7. ✅ 已完成 — 模型管理界面无法拖拽供应商及供应商内模型的先后顺序

### 问题描述
模型管理界面中，供应商列表之间无法拖拽调整先后顺序；供应商内部的模型条目同样无法拖拽排序。admin 和 hfadmin 两类账号均存在。

备注：与已解决的问题5（模型排序）范围不同——问题5是顶层模型列表排序，本问题是供应商分组及组内模型的排序。参考已实现的 bindProviderDrag/bindModelDrag 与 PUT /models/reorder 端点扩展

### 复现步骤
1. 打开模型管理
2. 尝试拖拽供应商卡片或其内部模型条目
3. 无拖拽响应或顺序不变

### 期望行为
- 支持拖拽供应商调整顺序、拖拽供应商内模型调整顺序，顺序持久化保存

### 影响范围
- admin + hfadmin 双面板

## 8. ✅ 已完成 — 模型池内拖动排序时界面闪烁

### 问题描述
模型池内拖动模型/子池调序时，操作过程中界面会闪烁一下。

### 复现步骤
1. 进入模型池页面
2. 拖动任一模型或子池
3. 松开瞬间界面闪烁

### 期望行为
- 拖拽过程平滑无闪烁

### 影响范围
- 模型池页面（双前端）

## 9. ✅ 已完成 — 拖动模型池时页面重新加载且不支持拖拽自动滚动

### 问题描述
1. 拖动模型池时会触发页面重新加载；
2. 拖拽不能跟随鼠标上下移动自动滚动页面——只能在可视区域内拖拽，无法把元素拖到视口外更上/更下的位置。

注：模型池内排序不需要自动滚动；但模型管理的供应商拖拽需要（供应商多于一屏时拖不到底部）。

### 复现步骤
1. 进入模型池页面 → 长按拖动 → 观察页面刷新
2. 将鼠标移向视口上下边缘 → 页面不滚动

### 期望行为
- ①拖动不触发整页刷新
- ②拖到视口边缘时自动滚动（至少模型管理界面）

### 影响范围
- 模型池 + 模型管理两处拖拽交互

## 10. ✅ 已完成 — hfadmin 点击编辑/添加等按钮时页面闪烁重载

### 问题描述
hfadmin 账号下，点击很多操作按钮（如编辑、添加等）都会导致界面闪烁并重新加载整页，体验不佳。

根因猜测（供排查）：问题8/9/10的"闪烁+整页重载"可能是同一根因——按钮或drop事件触发了form submit默认行为或location.reload/全量re-render，建议一并排查。

### 复现步骤
1. hfadmin 登录
2. 点击编辑/添加等按钮
3. 界面闪烁、整页重载

### 期望行为
- 按钮操作应局部刷新，不触发整页重载

### 影响范围
- hfadmin 面板全部管理操作

## 11. ✅ 已完成 (v2.7.2) — 父池调用时子池 json_output 标记未透传

### 问题描述
当外部客户端通过 `response_format={"type":"json_object"}` 请求 JSON 格式输出时，网关在解析**父池**（如 `auto`）时会报错：

```
{"detail":"请求要求 JSON 格式输出，但该池没有任何支持格式输出（json）的模型。请在模型管理勾选"支持格式输出（json）"（默认不支持）"}
```

然而父池引用的子池中**实际存在多个 `json_output=True` 的模型**。例如 `auto` 池的引用链：

```
auto
  → SenseTime全家桶
    → DeepSeek-V4-Flash(SenseTime)   ← 含3个 json_output=True 的模型
    → GLM-5.2(SenseTime)             ← 含3个 json_output=True 的模型
    → SenseNova-6.8-Flash-Lite(SenseTime) ← 含3个 json_output=True 的模型
  → Agnes-2-Flash
  → GLM-FREE全家桶
  → Qwen全家桶
```

SenseTime 全家桶的 3 个子池共 9 个模型都开了 `json_output=True`，但父池 `auto` 调用时，网关**没有透传 json_output 标记到子池**，导致检测到"池内无支持 JSON 输出的模型"。

**临时绕过**：直接用子池名 `DeepSeek-V4-Flash(SenseTime)` 而非父池名 `auto` 即可正常调用。

### 复现步骤
```bash
# 用 auto 池 → 503 错误
curl -s -X POST "http://127.0.0.1:8650/v1/chat/completions" \
  -H "Authorization: Bearer 200202" \
  -H "Content-Type: application/json" \
  -d '{"model":"auto","messages":[{"role":"user","content":"返回JSON"}],"response_format":{"type":"json_object"}}'
# → {"detail":"请求要求 JSON 格式输出，但该池没有任何支持格式输出（json）的模型..."}

# 用子池名 → 正常
curl -s -X POST "http://127.0.0.1:8650/v1/chat/completions" \
  -H "Authorization: Bearer 200202" \
  -H "Content-Type: application/json" \
  -d '{"model":"DeepSeek-V4-Flash(SenseTime)","messages":[{"role":"user","content":"返回JSON"}],"response_format":{"type":"json_object"}}'
# → 正常返回 JSON
```

### 期望行为
- 父池（如 `auto`）收到 `response_format={"type":"json_object"}` 时，应**递归遍历子池**，找到 `json_output=True` 的模型并路由
- 不应仅检查顶层池内是否直接包含 `json_output=True` 的模型
- 客户端不应该需要知道子池名才能用 JSON 模式

### 影响范围
- 池路由逻辑（`pool_resolver` 或类似模块）
- 所有使用父池名 + `response_format` 的外部客户端
- Mem0 等依赖 JSON 格式输出的第三方插件

## 12. ✅ 已完成 (v2.7.2) — embedding 模型在主界面显示为纯文本

### 问题描述
在模型网关管理界面中，将模型的 modality 设置为 `embedding` 后，主界面（模型列表/卡片）仍然将该模型显示为普通文本模型（`text`）的样式，没有区分嵌入模型与文本模型的视觉标识。

### 期望行为
- embedding 模型应在主界面用**不同颜色**（如绿色/紫色/蓝色等）与纯文本模型区分
- 模型卡片/列表项应显示 modality 标签（`text` / `embedding` / `vision`）
- 用户一眼能看出哪些是嵌入模型、哪些是文本模型

### 复现步骤
1. 登录网关管理界面（/admin 或 /hfadmin）
2. 进入模型管理
3. 添加或编辑一个模型，设置 modality 为 `embedding`
4. 保存后返回主界面
5. 观察该模型的显示样式——与 `text` 模型完全一样，无视觉区分

### 影响范围
- 网关管理界面前端（`static/index.html` + `static/hfadmin.html`）
- 模型卡片/列表渲染逻辑
- 用户体验：无法快速识别嵌入模型，尤其当模型数量多时

## 13. ✅ 已完成 (v2.7.2) — 测试连通性对嵌入模型发 chat/completions 请求致 400

### 问题描述
在网关管理界面点击「测试连通性」时，对 **embedding 模型**（如 `BAAI/bge-m3`，modality=`embedding`）仍然发送 `POST /v1/chat/completions` 请求。嵌入模型不支持 chat 接口，上游返回 400 Bad Request：

```
Client error '400 Bad Request' for url 'https://api.siliconflow.cn/v1/chat/completions'
For more information check: https://developer.mozilla.org/en-US/docs/Web/HTTP/Status/400 · 152ms
```

根因：测试连通性逻辑没有根据模型 modality 区分请求类型——对 `text` 模型发 chat/completions（正确），对 `embedding` 模型应该发 `/v1/embeddings` 但实际也发了 chat/completions（错误）。

### 期望行为
- 测试连通性时应根据模型 modality 选择正确的测试端点：
  - `text` 模型 → `POST /v1/chat/completions`（发一条简单消息）
  - `embedding` 模型 → `POST /v1/embeddings`（发一句短文本嵌入）
  - `vision` 模型 → `POST /v1/chat/completions`（带图片URL）
- 测试通过后显示"连通正常"，而不是 400 错误

### 复现步骤
1. 登录网关管理界面
2. 进入模型管理，找到一个 modality=`embedding` 的模型（如 BAAI/bge-m3）
3. 点击「测试连通性」
4. 观察返回：`400 Bad Request` —— 因为对嵌入模型发了 chat 请求

### 影响范围
- 测试连通性的后端逻辑（应根据 modality 分流）
- 前端测试按钮的请求构造
- 用户体验：嵌入模型永远显示"连通失败"，但实际可用（Mem0 已验证 bge-m3 嵌入正常工作）

## 14. ⚠️ 待复现确认 — auto 池内无法拖拽移动池内模型和子池顺序

### 问题描述
在网关管理界面中，**auto 父池**内部包含多个子池（如 `SenseTime全家桶`、`Agnes-2-Flash`、`GLM-FREE全家桶`、`Qwen全家桶`）。当前无法通过拖拽来调整这些子池的排列顺序，也无法拖拽 auto 池内的直接模型。

对比之下，问题 4（hfadmin 无法拖动模型池内模型或子池顺序）已在 v2.3bate 中解决，但 auto 池的拖拽功能似乎未覆盖到。

### 当前状态
```
auto 池配置:
{
  "strategy": "sequential",
  "auto_order": false,
  "model_ids": [
    "pool:SenseTime全家桶",
    "pool:Agnes-2-Flash",
    "pool:GLM-FREE全家桶",
    "pool:Qwen全家桶"
  ],
  "fallback_pool": "兜底池"
}
```

auto 池内的子池顺序直接影响请求路由优先级——`sequential` 策略下按 model_ids 数组顺序依次尝试。但用户无法通过 UI 拖拽调整顺序。

### 期望行为
- auto 池内的子池（`pool:xxx` 条目）应支持拖拽排序
- 拖拽后顺序应保存到 config.json
- 与问题 4 已修复的拖拽逻辑保持一致

### 复现步骤
1. 登录网关管理界面（/hfadmin）
2. 进入池管理，找到 auto 池
3. 尝试拖拽 auto 池内的子池条目（如 `SenseTime全家桶`）
4. 观察：拖拽无响应，或拖拽后刷新恢复原状

### 影响范围
- 池管理界面拖拽功能（`static/hfadmin.html`）
- 父池（auto）的子池排序
- 用户体验：无法调整父池内的子池优先级顺序

## 15. ✅ 已确认生效 (v2.7.2 实测) — 均衡负载 (load_balance) 逻辑疑问

### 问题描述
网关 config.json 中，每个池都有 `load_balance` 配置项。部分池设为 `True`，部分为 `False` 或 `None`：

```json
// 设置为 True 的池
SenseTime全家桶:  load_balance=True
GLM-5.2(SenseTime): load_balance=True
DeepSeek-V4-Flash(SenseTime): load_balance=True
SenseNova-6.8-Flash-Lite(SenseTime): load_balance=True

// 设置为 False 或 None 的池
auto: load_balance=None
Qwen全家桶: load_balance=False
Json-mini: load_balance=False
```

所有池的 `strategy` 均为 `sequential`（顺序策略）。

### 疑问点
1. **`strategy: sequential` + `load_balance: True` 是否矛盾？** — `sequential` 策略是"按顺序依次尝试"，`load_balance` 通常意味着"在多个可用模型间轮询/随机分配"。两者同时设置时，实际执行的是哪个逻辑？
2. **多模型池的 load_balance 是否生效？** — 例如 `SenseTime全家桶` 池内有多个模型，设为 `load_balance: True`，但请求是否真的在多个模型间轮询，还是仍然按顺序只用第一个？
3. **UI 开关是否对应后端逻辑？** — 管理界面中的"负载均衡"开关，是否在后端路由逻辑中实际生效？还是仅作为配置保存但无代码实现？

### 需要验证
- 在一个 `load_balance: True` 且含多个模型的池中，连续发送多次请求，观察请求是否实际分配到不同模型
- 查看路由代码（`router.py` 或类似文件），确认 `load_balance` 字段是否被读取和使用
- 对比 `sequential` 策略与 `load_balance` 开关的交互逻辑

### 期望行为
- `load_balance: True` 时，应真正在池内多个可用模型间分配请求（轮询/随机/最少连接）
- `load_balance: False` 或 `None` 时，严格按 `sequential` 顺序使用第一个可用模型
- UI 中的负载均衡开关应明确反映后端实际行为

### 影响范围
- 后端路由逻辑（请求分配策略）
- 池管理 UI（负载均衡开关）
- 用户体验：以为开启了负载均衡但实际仍在用单一模型

> 🔍 **2026-08-26 排查结论**：双前端池内条目（含 `pool:` 子池引用）拖拽与持久化均已实现（hfadmin `poolItemHTML`/`bindDrag`、index `poolItemHTML`/`bindDrag`，保存走 `PUT /admin/pools/{name}`）。未能在当前版本复现"无法拖拽"。请在新版（v2.7.2+）重试；若仍复现，请提供：①使用的面板（/admin 或 /hfadmin）②拖拽的具体条目 ③浏览器控制台报错。

---

## 16. ⚠️ 待修复 — 模型编辑页未根据计费类型条件显示/隐藏字段

### 问题描述
模型编辑/新增页面中，所有字段一次性全部展示，不随 `token_type`（计费类型）的变化而动态调整。导致用户面对不相关的字段，体验不佳，且容易填错。

### 当前行为（错误）
- 选择 `one_time`（一次性）时：仍显示"每日刷新时间"和"每日请求上限"字段
- 选择 `5h` 计费时：仍显示"每日刷新时间"字段
- 选择 `daily` 时：所有字段均展示

### 期望行为
| token_type | 应显示的字段 | 应隐藏的字段 |
|---|---|---|
| `daily` | 每日刷新时间、每日请求上限、每日 token 上限 | 一次性专用字段 |
| `one_time` | 总 token 上限、到期时间 | 每日刷新时间、每日请求上限 |
| `5h` | 5h 内请求上限、5h 内 token 上限 | 每日刷新时间 |

同时需合理化分配表单布局：相关字段就近排列，减少垂直滚动；**手机版响应式适配**（小屏时单列布局，字段标签不换行）。

### 影响范围
- `static/index.html` 与 `static/hfadmin.html` 的模型编辑弹窗/表单
- 移动端浏览器访问管理界面时
- 用户模型管理时的填写体验

### 复现步骤
1. 打开模型管理 → 新增模型
2. 切换"计费类型"下拉选项
3. 观察页面：字段列表不变，全部展示

---

## 17. ⚠️ 待修复 — 拖拽池和模型时强制刷新整页且多次提示"已改变顺序"

### 问题描述
1. 拖拽模型池内条目（模型/子池）时，页面强制整页刷新（白屏闪烁）
2. 拖拽一次操作，弹出多次"已改变顺序"的 toast 提示
3. 拖拽体验不流畅

### 期望行为
- 拖拽过程中不触发整页刷新，局部 DOM 更新
- 一次拖拽操作只触发一次保存请求 + 一次 toast 提示
- 参考已修复的问题 8/9/10 的模式

### ⚠️ 严格约束
**禁止引入"拖拽后数据消失"bug**——以前曾出现拖拽后刷新页面数据丢失的严重问题，修复时必须保证拖拽持久化可靠。

### 影响范围
- `static/index.html` 与 `static/hfadmin.html` 中所有拖拽交互
- 池管理页面的拖拽排序
- 模型管理页面的拖拽排序

### 复现步骤
1. 进入池管理
2. 拖拽任一池内条目
3. 观察：页面整页刷新 + 多次 toast

---

## 18. ⚠️ 待优化 — token 用量计算准确度不足

### 问题描述
当前网关统计 token 用量时，准确度不够高。可能存在：
- 只按响应返回的 `usage` 字段记录，未考虑缓存命中等边界情况
- 并发请求时计数不准确
- 部分失败请求的 token 消耗未正确扣除

### 期望行为
- 按上游实际返回的 `usage.prompt_tokens` + `usage.completion_tokens` 精确记录
- 并发场景下计数准确
- 失败请求的已消耗部分正确计入

### 影响范围
- `main.py` 中响应处理逻辑
- `pool.py` 中 quota 追踪逻辑
- `database.py` 用量记录

### 复现步骤
- 发送高并发请求 → 观察用量统计与实际是否一致
- 触发 RPM/TPM 限流 → 观察计数是否在临界点准确

---

## 19. ⚠️ 待修复 — 模型达到限额后仍被路由尝试（每次被拦截再轮转下一个）

### 问题描述
当池内某个模型已经达到用量上限（`daily_token_limit` / `rpm_limit` / `tpm_limit` 触顶），路由时仍然会先尝试调用该模型，被上游拦截返回错误后，再轮转到下一个可用模型。这一过程**白白消耗了一次请求的时间和网络开销**，导致实际调用延迟显著增加。

### 期望行为
- 路由前应**预检**各模型的限额状态，直接排除已达上限的模型，不发起实际请求
- 预检结果可短时缓存（如缓存 5 秒），避免每次请求都查询数据库
- 模型限额恢复到可用状态时，重新加入候选

### 影响范围
- `pool.py` 中模型路由 / 池内选择逻辑
- `main.py` 中请求分发逻辑
- 用户体验：请求延迟显著增加（每次多花一轮被拦截再重试的时间）

### 复现步骤
1. 设置某模型 `daily_token_limit` 为较低值
2. 使用该模型直到达限
3. 通过包含该模型的池发送请求
4. 观察：路由先尝试已达限的模型 → 被拦截 → 轮转下一个

---

## 20. ⚠️ 待修复 — auto_order 与 load_balance 前端未互斥，可同时开启

### 问题描述
在池管理界面中，"自动择优"（`auto_order`）和"负载均衡"（`load_balance`）两个开关**可以同时开启**，没有互斥约束。但后端逻辑中二者语义冲突：
- `auto_order: True` → 按调用延迟从低到高择优
- `load_balance: True` → 请求均匀轮转分配

两者同时开启时行为未定义或互相干扰。

### 当前行为（错误）
- 前端两个开关完全独立，可自由组合
- 后端 `admin.py` 创建池时已有互斥逻辑（`load_balance` 开启时强制 `auto_order=False`），但**更新池**（`PUT /admin/pools/{name}`）时**未做互斥校验**
- 用户通过更新接口可绕过创建时的互斥，造成双开状态

### 期望行为
- **前端**：`auto_order` 开启时 `load_balance` 自动置灰不可选；`load_balance` 开启时同理
- **后端**：`PUT /admin/pools/{name}` 和 `PUT /admin/pools/{name}/model` 更新接口同样做互斥校验，开启一方时自动关闭另一方
- 可以同时关闭（默认顺序调用），但不能同时开启
- 前端 toast 提示用户"已为您关闭另一个开关"

### 影响范围
- `static/index.html` 与 `static/hfadmin.html` 的开关交互
- `admin.py` 池更新接口
- 后端池配置状态一致性

### 复现步骤
1. 打开池管理
2. 将"自动择优"开关打开
3. 再将"负载均衡"开关打开
4. 观察：两个开关同时处于开启状态
