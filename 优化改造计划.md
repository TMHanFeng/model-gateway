# 模型网关提速与轻量化改造计划

> 日期：2026-09-09 ｜ 基线版本：v2.11.30 ｜ 原则：更快、更轻量，**不与原有逻辑冲突**——保持"响应返回前计费已落盘"契约（42 个回归测试依赖）、功能行为语义不变、每批次回归全绿。

## 一、结论摘要（实测量化）

| 实测项 | 数值 | 说明 |
|---|---|---|
| SQLite 单笔 commit | 0.101 ms | WAL+NORMAL，临时库实测；SQLite 本身不慢 |
| 合并事务后每笔摊销 | 0.039 ms | N 笔 1 事务，收益约 2.6 倍 |
| `/stats` 端点耗时 | 55~58 ms | 48 模型 ≈ 250 次锁内查询，面板每 5 秒轮询 |
| 流式请求结算 | 6~8 个独立事务 | 非流式经 v2.10.8 已合并为 1~2 个 |
| WAL 文件 | 4.2 MB（库本体 1.4 MB） | 持续读者导致 checkpoint 长期完不成 |
| httpx keepalive | 默认 5 秒过期 | 空闲后每请求重新 TCP+TLS 握手 |

**瓶颈不是 SQLite，而是**：全局锁把读写全部串行（`database.py:46`）＋ 流式未合并事务 ＋ `/stats` 轮询与线上请求争锁 ＋ 连接频繁重建。

## 二、问题清单（按严重度，含证据）

### A. 正确性缺陷（必须先修）

**A1. `bulk()` 写合并在并发下不正确**
`_bulk` 是模块级全局计数器（`database.py:49`），两个热路径调用方——非流式 `pool.py:866` 与用户 Key 计费 `keyauth.py:143`——分属不同请求协程，并发下必然交错：
- **延迟持久**：A 的计费写靠 B 的 bulk 顺手提交，A 返回 200 后若崩溃则计费丢失；
- **回滚越界**：最外层 bulk 异常回滚会连带回滚无关请求的计费；
- **部分提交**：非最外层 bulk 内部异常不回滚，半截写入被后续提交者带上车；
- **取消悬空**：`except Exception` 接不住 CancelledError（客户端断连），事务既不提交也不回滚。

**A2. 死代码**：`update_decision_actual_tokens`（database.py:717）全项目无调用方。

### B. 性能损耗

| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| B1 | 流式结算未合并：`_settle_stream_tokens` 3 笔 + `add_call_metric` + `log_decision` + Key 计费，各独立事务 | pool.py:998-1012, 976, 985 | 每流式请求 6~8 次锁竞争 + commit |
| B2 | `/stats` N+1 串行查询，每模型 4~8 次 | pool.py:1599-1698 | 55ms/次 × 每 5 秒，与请求路径抢锁 |
| B3 | request_log 表仅为 60 秒滑窗 RPM/TPM 存在，每请求付 1 INSERT + 1 全表扫 DELETE（timestamp 无索引），读时再 2 次查询 | database.py:537-572 | 纯开销，可整体内存化 |
| B4 | decision_log / call_metrics 逐笔裁剪 DELETE 随每笔写执行 | database.py:710-712, 739-743 | 每请求多 2 条语句 |
| B5 | httpx 连接池 keepalive 5 秒过期 | openai_provider.py:23、anthropic_provider.py:18 | 低频场景每请求重握手，TTFB 直接受损 |
| B6 | `_wrap_key_stream` 每 chunk 全量 json.loads（pool 层早有 `'"usage"' in chunk` 预筛，此处遗漏） | main.py:105 | key_user 流式每 chunk 多一次解析 |
| B7 | `/admin/keys` 每 Key 重复查同一行 2 次（summary 与 available 各查一次） | keyauth.py:104, 174, 193 | 2N+1 查询 |
| B8 | `_has_images` 每请求重复扫描两遍 | main.py:189 + pool.py:1262/1404 | 多模态请求重复遍历 |
| B9 | `reload()` 直接 clear 旧 providers，不 aclose，泄漏连接池 | pool.py:300-309 | 每次热加载泄漏 N 个客户端 |
| B10 | WAL 膨胀无维护 | gateway.db-wal 4.2MB | 写放大 |
| B11 | 两面板无 visibilitychange，后台标签页照常 5 秒轮询；HTML 路由每次同步读 150KB+ 文件 | hfadmin.html:1054-1055、admin.py:75、main.py:488 | 无效负载 |

## 三、分批实施计划

### 批次 0｜修复 bulk() 并发语义（正确性前置，不修不能继续）

`database.py` 改造（其余调用方零改动）：

```python
@asynccontextmanager
async def bulk():
    global _bulk
    conn = await _get_conn()
    if _bulk == 0:
        await _lock.acquire()          # 仅最外层进门抢锁，全程持有
    _bulk += 1
    try:
        yield conn
        if _bulk == 1:
            await conn.commit()
    except BaseException:              # 覆盖 CancelledError
        if _bulk == 1:
            try: await conn.rollback()
            except Exception: pass
        raise
    finally:
        _bulk -= 1
        if _bulk == 0:
            _lock.release()            # 最外层退出释放
```

- 各读写函数改用 `_maybe_lock()` helper：`_bulk > 0` 时（已在 bulk 锁内）不重复加锁，否则照旧 `async with _lock`。
- **约束**：bulk 只包 DB 语句（~1ms），绝不跨上游 HTTP 调用（现有两个调用点均满足；后续批次同样遵守）。
- 回归重点：T7（十并发精确总额 delta==1330）、全部流式用例。

### 批次 1｜流式结算合并（6~8 commit → 1~2）

- `pool.py` `_wrap_stream` 的 finally：`_settle_stream_tokens` + `add_call_metric` + `log_decision` 三段包进一个 `db.bulk()`。
- **同步完成、不后台化**——保住"最后一个字节发出后、响应收尾前落盘"的时序（T1c/T2a 断言依赖）。
- 非流式保持现状 2 commit（execute bulk + decision 独立），不跨层重构。

### 批次 2｜热路径语句瘦身

- **RPM/TPM 内存化**：request_log 表唯一用途是 60 秒滑窗计数（消费方仅 `get_rpm`/`get_tpm`），改为每模型内存 deque：
  - 删除 `log_request` 的 INSERT+DELETE；`get_rpm`/`get_tpm` 读内存，零锁零查询；
  - 语义差异仅一点：网关重启后 60 秒限流窗口清零（限额判定逻辑不变）；
  - 测试配套：T5d 断言改经 `/stats` 的 `current_rpm`；deep_clean 移除该表条目；**新增 1 条 RPM 触顶 429 回归用例**（顺带填补现有测试空洞：RPM/TPM 限额从无触顶测试）。
- decision_log / call_metrics 的逐笔裁剪 DELETE 移到 scheduler 每 60 秒批量执行（有界性不变，读取走 `ORDER BY id DESC LIMIT` 不受影响）。

### 批次 3｜/stats 批量化（~250 次锁内查询 → ~10 次）

- `database.py` 新增聚合快照函数：`model_daily_stats WHERE date IN (今,昨)` 一条、`rolling5h_state`/`one_time_state`/`gift_state` 小表各一条、RPM/TPM 读内存聚合。
- `pool.get_stats` 改用快照；**仅 6 个 gift 模型保留逐个 `get_gift_balance`**（惰性快照/补账写语义原样保留，不与校准/补账逻辑冲突）。
- 预期 55ms → <5ms，面板轮询退出与线上请求的锁争抢。

### 批次 4｜传输层与杂项（零风险快赢）

1. 两 provider `httpx.Limits` 加 `keepalive_expiry=300`（各一行）；
2. `_wrap_key_stream` 加 `'"usage"' in chunk` 预筛（对齐 pool.py:925 既有做法）；
3. `_has_images` 结果缓存到 req 实例；
4. `reload()` 旧 providers 收集后后台 `aclose()`；
5. scheduler 新增维护任务：每 10 分钟 `PRAGMA wal_checkpoint(TRUNCATE)`；
6. `/admin`、`/hfadmin` 页面路由改 mtime 缓存读；两面板加 visibilitychange 暂停/恢复轮询；
7. 删除死代码 `update_decision_actual_tokens`。

## 四、明确不做项（防与原有逻辑冲突）

| 不做项 | 原因 |
|---|---|
| 读写连接分离 | `get_daily_usage`/`get_gift_balance` 是"读中带写"（跨窗口惰性 UPDATE），拆连接语义分叉；批次 3 后读量已很小 |
| 内存账本 + 延迟批量落盘 | 30+ 测试用外部连接在响应后立即断言，延迟落盘破坏"响应前已持久"契约 |
| Anthropic 转换主体 / 请求透传架构优化 | 与计费、协议转换语义深度耦合，收益配不上风险 |
| verify_key 缓存 | Key 启停/轮换需即时生效，且单次查询仅 ~0.1ms |

## 五、验证与提交

- 每批一次提交，沿用 v2.11.x 版本号与提交信息风格（批次 2 起 v2.11.31+）；
- 每批后跑 `test_billing_regression.py` 全量（批次 2 后 43+ 用例）；
- 全部完成后手动验证：流式对话一次、`/stats` 计时对比（目标 <5ms）、gift 模型预检与人工校准链路、热加载后旧连接释放。

## 六、预期收益汇总

| 指标 | 现状 | 目标 |
|---|---|---|
| 流式请求 DB 事务数 | 6~8 | 1~2 |
| 每请求 DB 语句数 | ~10 | ~4 |
| `/stats` 耗时 | 55ms | <5ms |
| 低频请求 TTFB | 含 TLS 重握手 | 复用长连接 |
| WAL 体积 | 持续膨胀（4.2MB+） | 稳定在 MB 级以内 |
