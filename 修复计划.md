# Model Gateway 完整修复计划

**生成日期**：2026-08-20  
**目标 HEAD**：`653e9ce`  
**范围原则**：**只修 6 个已记录问题，不重构、不优化、不动无关代码**

> 问题描述见同目录 `问题待修复.md`。本文件是修复的执行手册。

---

## 总体执行顺序

| 阶段 | 问题 | 类型 | 文件 | 预计工时 | 风险 |
|---|---|---|---|---|---|
| Phase 1 | 问题 5 | 后端补端点 | `admin.py` | 15 min | 极低 |
| Phase 2 | 问题 6 诊断 | 后端加日志 | `providers/openai_provider.py` + `main.py` | 10 min | 极低 |
| Phase 3 | 问题 4 | 前端双面板 | `static/hfadmin.html` + `static/index.html` | 60-90 min | 中 |
| Phase 4.1 | 问题 1 | 前端双面板 | `static/hfadmin.html` + `static/index.html` | 30-60 min | 中 |
| Phase 4.2 | 问题 2 | 验证+修 | `format_adapter.py` / `main.py` | 15-30 min | 低 |
| Phase 4.3 | 问题 3 | 验证+修 | `keyauth.py` / `database.py` | 15-30 min | 中 |
| Phase 5 | 问题 6 实际修复 | 后端 | `main.py` / `format_adapter.py` / `providers/*.py` | 待 Phase 2 结论 | 高 |

**执行顺序**：Phase 1 → Phase 2 → Phase 3 → Phase 4.1 → Phase 4.2 → Phase 4.3 → Phase 5

---

## Phase 1 — 补 `/admin/models/reorder` 端点（修问题 5）

**根因（高置信度）**：`admin.py` 完全缺少 `/admin/models/reorder` 路由。只有 `providers/reorder` (line 284) 和 `pools/reorder` (line 379)，没有 models 的。hfadmin 调用必然 404 → 顺序无法持久化。

**改动**：`admin.py`（仅追加，不动现有代码）

**插入位置**：`reorder_pools` 函数定义后（`admin.py:405` 后）

**新增端点**：

```python
@router.put("/models/reorder")
async def reorder_models(request: Request, _=Depends(verify_admin)):
    """Reorder models. Body: {model_ids: [...]} must contain exactly all existing model IDs."""
    body = await request.json()
    new_order = body.get("model_ids", [])
    if not isinstance(new_order, list):
        raise HTTPException(status_code=400, detail="model_ids must be a list")

    config = load_config()
    models = config.get("models", [])
    existing_ids = {m["id"] for m in models}
    if set(new_order) != existing_ids:
        missing = existing_ids - set(new_order)
        extra = set(new_order) - existing_ids
        raise HTTPException(
            status_code=400,
            detail=f"model_ids must contain exactly all existing model IDs. Missing: {sorted(missing)}, Extra: {sorted(extra)}",
        )

    by_id = {m["id"]: m for m in models}
    config["models"] = [by_id[mid] for mid in new_order]
    save_config(config)
    restart_scheduler()
    _sync_pool()
    return {"ok": True}
```

**验证**：
```bash
curl -X PUT http://127.0.0.1:8650/admin/models/reorder \
  -H "Authorization: Bearer 123456" \
  -H "Content-Type: application/json" \
  -d '{"model_ids":["openai/gpt-4o-once"]}'
# 期望：{"ok":true}
```

---

## Phase 2 — 诊断日志定位问题 6

**目的**：在不改业务逻辑前提下，加 DEBUG 日志区分"上游没生成 content" vs "网关丢失 content"。

### 2.1 `providers/openai_provider.py`

**插入位置**：`chat` 方法中 `data = resp.json()` (line 75) 后

**新增代码**：
```python
# === Issue 6 诊断日志（DEBUG 级别，默认不输出）===
import logging
logger = logging.getLogger(__name__)
if logger.isEnabledFor(logging.DEBUG):
    logger.debug(f"[openai upstream raw] model={model_name} status={resp.status_code} body={data}")
# Issue 6: 上游有 completion_tokens 但 content 为空 — 始终打 WARNING（无需 DEBUG）
try:
    _first = (data.get("choices") or [{}])[0]
    _msg = _first.get("message") or {}
    _content = _msg.get("content")
    _tokens = (data.get("usage") or {}).get("completion_tokens", 0)
    if _tokens > 0 and not _content:
        logger.warning(
            f"[openai empty content] model={model_name} completion_tokens={_tokens} "
            f"finish_reason={_first.get('finish_reason')} content_repr={repr(_content)[:200]} "
            f"has_tool_calls={bool(_msg.get('tool_calls'))}"
        )
except Exception:
    pass
```

### 2.2 `main.py`

**插入位置 1**：文件顶部 import 块（line 14 后）

**新增代码**：
```python
import logging
logger = logging.getLogger(__name__)
```

**插入位置 2**：`_chat_handler` 非流式分支 (line 169 `response, tokens, steps = ...` 后)

**新增代码**：
```python
# === Issue 6 诊断日志（DEBUG 级别，默认不输出）===
if logger.isEnabledFor(logging.DEBUG):
    logger.debug(f"[gateway return] {response.model_dump_json()[:500]}")
```

**验证**：在 main.py 启动前设 `logging.basicConfig(level=logging.DEBUG)`，跑用户复现命令，对比日志中"上游响应"和"网关响应"的 content 字段。

---

## Phase 3 — 双前端拖拽修复（修问题 4）

**根因**：hfadmin 拖拽事件可能未正确触发；/admin 完全没有拖拽（commit `31da20b` 之后零改动）。

**改动文件**：
- `static/hfadmin.html`（1325 行）
- `static/index.html`（同行数级别）

**步骤**：
1. 在两文件中 grep `Sortable|draggable|dragstart|dragover|dragend|onDrag` 定位拖拽代码
2. 仅读相关段（不全读）
3. 检查 hfadmin：拖拽事件绑定是否正确、API 调用 URL 是否对
4. /admin 完全没有拖拽控件：参考 hfadmin 已有逻辑补齐
5. 修复后手动两面板 e2e 测试

**双前端要求**：
- /hfadmin：修复事件绑定使拖拽可触发，并调用新加的 `/admin/models/reorder`（问题 5）
- /admin：补全与 /hfadmin 同等的拖拽 UI 与调用

---

## Phase 4.1 — 统计面板维度切换 bug（修问题 1）

**根因**：切换 token 用量 ↔ 调用次数时，token 数被当调用次数展示。

**改动文件**：
- `static/hfadmin.html`
- `static/index.html`

**步骤**：
1. grep `stats|token|调用次数|token 用量` 定位统计面板 JS
2. 找维度切换函数
3. 检查是否错误地复用了同一展示函数

**双前端要求**：两套面板分别修。

---

## Phase 4.2 — Anthropic 格式支持验证（修问题 2）

**说明**：git log `e656fde` 显示 v2.0.6bate 已修复，但问题清单仍列出——需验证是否真的完整。

**验证命令**：
```bash
curl -X POST http://127.0.0.1:8650/v1/messages \
  -H "x-api-key: 123456" \
  -H "anthropic-version: 2023-06-01" \
  -H "Content-Type: application/json" \
  -d '{"model":"auto","max_tokens":100,"system":"hi","messages":[{"role":"user","content":"hi"}]}'
```

**期望**：返回 Anthropic 格式响应（content 是 block 列表，type=message 等）。

**如果失败**：
- 检查 `main.py:114 is_anthropic_request`
- 检查 `format_adapter.py` 的 `anthropic_to_openai` / `openai_to_anthropic_response` / `openai_sse_to_anthropic`
- 修复具体 bug

---

## Phase 4.3 — API Key 自动轮换验证（修问题 3）

**说明**：git log `e656fde` 显示 v2.0.6bate 已修复，但问题清单仍列出——需验证。

**验证步骤**：
```bash
# 1. 创建 expire_seconds=10 的测试 key
curl -X POST http://127.0.0.1:8650/admin/keys \
  -H "Authorization: Bearer 123456" \
  -H "Content-Type: application/json" \
  -d '{"name":"__test_rotation__","type":"user","expire_seconds":10,"allowed_pools":["auto"]}'
# 记录返回的 secret

# 2. 等待 12 秒

# 3. 用原 secret 调 API，应仍认证通过且 rotated_at 应被更新
curl -X POST http://127.0.0.1:8650/v1/chat/completions \
  -H "x-api-key: <原 secret>" \
  -H "Content-Type: application/json" \
  -d '{"model":"auto","messages":[{"role":"user","content":"hi"}]}'

# 4. 查 DB 看 rotated_at 是否已更新（手动用 sqlite3 工具或管理后台）
```

**如果失败**：
- 检查 `keyauth.maybe_rotate_expired`（main.py:58 调用）
- 检查 `database.py` 中 `rotated_at` 字段更新逻辑
- 修复具体 bug

**清理**：验证完成后删除测试 key。

---

## Phase 5 — 问题 6 实际修复

**待 Phase 2 诊断结果定方案**：
- **情况 A**：上游原始 content 就为空 → 不是网关 bug，仅加 WARNING 日志，告知用户检查上游模型/Sakura 隧道
- **情况 B**：网关丢失 content → 修 `main.py:178` 响应返回路径或 `format_adapter.py:156` 转换路径
- **情况 C**：两种日志都正常 → 问题在 Sakura 隧道或客户端解析层，网关无能为力

---

## 验证 checklist

- [ ] Phase 1: curl `/admin/models/reorder` → 200 OK；config.json 顺序改变
- [ ] Phase 2: DEBUG 日志区分上游 / 网关 / 隧道
- [ ] Phase 3: 双面板拖拽 → 刷新后顺序保持
- [ ] Phase 4.1: 双面板切换维度 → 数据正确
- [ ] Phase 4.2: curl `/v1/messages` → Anthropic 格式
- [ ] Phase 4.3: 测试 key 过期 → rotated_at 更新
- [ ] Phase 5: 复现命令 → content 不为空
- [ ] `lsp_diagnostics` 改过的 Python 文件无报错

## 风险与约束

1. **前端 HTML 都是 1300+ 行单文件**：先用 grep 定位相关段，不全读
2. **每个改动最小化**：不加 docstring、不重构、不顺手优化、不格式化
3. **改一处可能破坏另一处**：每个 Phase 完成后跑 `lsp_diagnostics` 验证
4. **不重启不生效**：前端改动需浏览器刷新（前端无构建）；后端改动需重启 main.py
5. **测试 key 用完清理**：Phase 4.3 创建的测试 key 验证后需手动删除