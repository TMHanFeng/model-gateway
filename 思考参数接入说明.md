# 思考档位接入说明（reasoning_effort）

> 适用版本：v2.10.0+　|　实测日期：2026-09-04　|　配套工具：`probe_reasoning.py`（探测）、`思考参数探测报告.md`（探测明细）

中转站对外暴露**统一的思考控制参数**，客户端不需要关心各上游模型（火山 / MiniMax / DeepSeek / 商汤 / GLM / anthropic 协议……）思考参数的差异，网关按每个模型配置的 `reasoning_map` 自动换算注入。

---

## 一、客户端怎么传参

### 1. OpenAI 格式（/v1/chat/completions）

请求体顶层加 `reasoning_effort`，六档（大小写不敏感）：

| 档位 | 含义 |
|:---|:---|
| `off` | 强制关闭思考（仅对支持关闭的模型生效，不支持的回落到最低思考档） |
| `minimal` | 最短思考 |
| `low` / `medium` / `high` | 递增的思考深度 |
| `max` | 最深思考（上游无此档时自动用其支持的最深档） |
| `auto` | 本次请求不注入参数、用模型默认行为（v2.10.3+；即服务端默认档为 low 时的"单次豁免"） |

```bash
curl -X POST http://127.0.0.1:8650/v1/chat/completions \
  -H "Authorization: Bearer your-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "auto",
    "reasoning_effort": "high",
    "messages": [{"role": "user", "content": "9.11 和 9.8 哪个大？"}]
  }'
```

Python（openai SDK）：

```python
resp = client.chat.completions.create(
    model="auto",
    messages=[{"role": "user", "content": "9.11 和 9.8 哪个大？"}],
    extra_body={"reasoning_effort": "high"},   # SDK 未收录的字段用 extra_body 透传
)
```

- **不传 `reasoning_effort` = 默认 `low` 档**（v2.10.2+）。默认档可在 config.json 顶层 `"default_reasoning_effort"` 调整（六档或 auto）；设为 `"auto"` 或 `""` = 不注入，恢复"模型默认思考"的旧行为；配置非法值回落 low。未配置 `reasoning_map` 的模型不受默认档影响（参数被忽略）。`auto` 既可配在服务端做全局默认，也可在单个请求里传 `"reasoning_effort": "auto"` 做单次豁免。
- 非法值返回 422，错误信息列出支持的档位。

### 2. Anthropic 格式（/v1/messages）

按 Anthropic 惯例传 `thinking`，网关自动换算成统一档位再走同一套映射：

| 传法 | 换算结果 |
|:---|:---|
| `"thinking": {"type": "disabled"}` | off |
| `"thinking": {"type": "enabled", "budget_tokens": N}` | ≤1024→minimal，≤4096→low，≤10240→medium，≤20480→high，>20480→max |
| `"thinking": {"type": "enabled"}`（未带 budget） | medium |

```json
{
  "model": "auto",
  "max_tokens": 20000,
  "thinking": {"type": "enabled", "budget_tokens": 16384},
  "messages": [{"role": "user", "content": "9.11 和 9.8 哪个大？"}]
}
```

### 3. 思考内容在响应里怎么看

| 客户端形态 | 思考内容位置 |
|:---|:---|
| OpenAI 非流式 | `choices[0].message.reasoning_content`（DeepSeek 风格字段；MiniMax 等 `<think>` 内联的模型已自动提取，正文不含标签） |
| OpenAI 流式 | `delta.reasoning_content` 增量（`<think>` 内联同样自动转换） |
| Anthropic 非流式 | `content` 中的 `{"type": "thinking", "thinking": "..."}` 块 |
| Anthropic 流式 | `content_block_delta` 的 `thinking_delta` |

---

## 二、回落规则（档位不全的模型怎么处理）

每个模型的 `reasoning_map` 只需配置它**真实支持**的档位。客户端请求了未配置的档位时：

1. 取**更低档位中最近**的配置档（如模型只有 off/medium/high，请求 `low` → 按 `off` 处理？不——`low` 比 `medium` 低，取 `off`？规则是取更低档里最近的：`low` 的更低配置档只有 `off` → 按 `off`）。请求 `max` 超过全部配置 → 取最高配置档 `high`。
2. 无更低配置档时（如请求 `off` 但模型没配 off），取**最低配置档**。
3. **未配置 `reasoning_map` 的模型：参数被忽略**，用模型默认行为，**请求不会报错**。

典型映射形态（探测自动生成，已写入 config.json）：

```json
// 火山 Doubao / DeepSeek 系：六档全支持（reasoning_effort），off 用 thinking 关闭
{"off": {"thinking": {"type": "disabled"}},
 "minimal": {"reasoning_effort": "minimal"}, "low": {"reasoning_effort": "low"},
 "medium": {"reasoning_effort": "medium"}, "high": {"reasoning_effort": "high"},
 "max": {"reasoning_effort": "max"}}

// MiniMax-M2.7-highspeed：接受档位但无法关闭思考 → 不配 off
{"minimal": {"reasoning_effort": "minimal"}, "low": {"reasoning_effort": "low"},
 "medium": {"reasoning_effort": "medium"}, "high": {"reasoning_effort": "high"},
 "max": {"reasoning_effort": "max"}}

// anthropic 协议（dots 等）：budget 阶梯
{"off": {"thinking": {"type": "disabled"}},
 "minimal": {"thinking": {"type": "enabled", "budget_tokens": 1024}},
 "low": {"thinking": {"type": "enabled", "budget_tokens": 4096}},
 "medium": {"thinking": {"type": "enabled", "budget_tokens": 10240}},
 "high": {"thinking": {"type": "enabled", "budget_tokens": 20480}},
 "max": {"thinking": {"type": "enabled", "budget_tokens": 32768}}}
```

anthropic 协议上游注入 `thinking.budget_tokens` 时，网关会自动抬高 `max_tokens`（budget+1024），满足上游硬性校验。

---

## 三、探测机制：新增模型自动探测，存量模型手动补测

### 新增模型 → 保存瞬间自动探测（v2.10.1+）

在面板新增模型（**embedding / rerank 模态除外**）保存后，网关自动处理思考档位：

1. **该上游组合之前探测过**（缓存 `reasoning_probe_cache.json` 命中）→ **瞬间自动套用**实测映射，面板提示"思考映射已按实测缓存自动套用"；
2. **新组合** → 立即启动后台探测（约 1~3 分钟），完成后**自动写入**该模型的 `reasoning_map` 并热重载，面板提示"思考档位后台探测中，完成后自动保存"；
3. 探测失败（上游不可达 / 配额不足等）只是没有映射，**不影响模型本身使用**（`reasoning_effort` 被忽略，走模型默认思考行为）。

面板中"思考映射"输入框对 embedding/rerank 模态自动隐藏；把模型模态改成 embedding/rerank 保存时会自动剥离已有映射。

### 存量模型 / 批量补测 → 手动脚本

```bash
python probe_reasoning.py                # 探测所有未缓存的模型，生成《思考参数探测报告.md》
python probe_reasoning.py --apply        # 把实测映射写入 config.json（含备份）并触发热重载
python probe_reasoning.py --filter minimax --force   # 只重测指定模型（--force 忽略缓存）
```

- 探测结果缓存在 `reasoning_probe_cache.json`（断点续跑，重复运行跳过已测组合，新增模型自动探测也复用这份缓存）。
- 上游更新参数后，对目标模型 `--force` 重测一次即可。

---

## 四、管理端怎么改（查询位置 = 模型编辑界面）

- **编辑弹窗内嵌探测结论条**（/admin 与 /hfadmin 双面板一致）：模型管理 → 点「编辑」，"思考映射"输入框下方显示该云端模型的探测结论——探测日期 · 参数族 · 默认思考开/关 · 六档梯度是否有效 · off 能否真正关闭；探测失败（429/403 等）会显示原因并引导重测；未探测的模型可一键补测。
- **📥 填入建议**：把探测建议映射填进输入框（框内已有自定义内容时先确认再覆盖）；**空框自动预填**探测建议并标注"可修改或清空"——建议只是辅助填写，保存与否、最终映射由你决定。
- **🔄 重新探测**：对当前模型的上游组合强制重测（约 30~60 秒，少量小额请求），结果只更新探测缓存、不自动改写映射；完成后点「⟳」刷新结论。模型列表的「思考映射」徽章可点击直达编辑框。
- **API**：
  - `GET /admin/reasoning`：全部模型的探测结论 + 生效映射 + 六档实际注入片段（纯本地读取，不发上游请求）；
  - `POST /admin/reasoning/probe`（body `{"model_id": "..."}`）：强制重测单个模型（后台执行）。
- **config.json**：模型条目加 `"reasoning_map": {...}`（如上格式），改完 `POST /admin/reload` 或重启生效。

---

## 五、全池六档实测结果（2026-09-04，经网关实测）

40 个 chat 类模型 × 6 档（off/minimal/low/medium/high/max），共 240 次真实调用。**"X"均为上游限流/配额问题（503 用量触顶、429、403、404），没有任何一档因思考映射本身失败**；无映射模型的六档同样全部通过（参数被忽略）。

| 模型 | 映射 | 六档实测 | 说明 |
|:---|:---|:---:|:---|
| HuoSan-Ark/Doubao-Seed-2.1-turbo·pro | off+六档 | **6/6** | |
| HuoSan-Ark/DeepSeek-V4-Flash正式版·Pro | off+六档 | **6/6** | |
| HuoSan-Ark/glm-5.2 | off+六档 | **6/6** | |
| HuoSan-Ark/Doubao-Seed-Character | 无 | **6/6** | 角色模型不思考，参数被忽略 |
| MiniMax/MiniMax-M3 | off+六档 | **6/6** | 档位梯度真实，可完全关闭 |
| MiniMax/MiniMax-M2.7-highspeed | 五档(无off) | **6/6** | off 回落最低档；上游不支持关闭思考 |
| deepseek/deepseek-v4-flash（含 vision） | off+六档 | **6/6** | |
| deepseek2、deepseeksam 同款 | off+六档 | **6/6** | |
| dots/dots3-note-prev | off+budget 六档 | **6/6** | anthropic 协议 |
| xiaoen/gemini-3.1-pro | 五档(无off) | **6/6** | off 回落最低档 |
| xiaoen/claude-opus-4-5 | off+六档 | **6/6** | |
| OpenRouter/nemotron-3.5-lightning:free | off+六档 | **6/6** | |
| Qwen3.8-27B / GLM-4.7-Flash / Ling-3.0-tiny（本地） | 五档(无off) | **6/6** | |
| shangtang~shangtang4 / shagntang3 的 deepseek·glm·sensenova 共 12 条 | off/五档 | 1/6 ~ 6/6 | 商汤按 key 限 RPM：**失败档随机分散、同上游不同 key 通过率不同**，重试即恢复；非档位问题 |
| xiaomi/mimo-v2.5-pro | 无 | 0/6 | 上游持续 429（配额），探测阶段即不可用 |
| deepseekf/deepseek-v4-flash（含 vision） | off+六档 | 0/6 | 该条目当日配额/上游限流，非档位问题 |
| qwen/deepseek-v4-flash-0731 | 无 | 0/6 | 上游 403（密钥） |
| arktokenplan/deepseek-v4-flash | 无 | 0/6 | 上游持续 429 |
| GLMAPI/glm-5.3-flash | 无 | 0/6 | 上游持续 429 |
| OpenRouter/ox-alpha | 无 | 0/6 | 上游 404（模型已下线） |

> 汇总：**29/40 六档全通**（含全部有映射的主流上游）；其余 11 条的失败均可归因于上游限流 / 配额 / 密钥 / 模型下线，恢复后重跑 `python probe_reasoning.py --filter 关键字` 补测映射即可。embedding/rerank 模态模型不适用思考参数，未参与测试。
