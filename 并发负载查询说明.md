# 并发负载查询接口说明（v2.12.0+）

查询**单个模型**（而非模型池）当前的并发负载：有多少请求正在上游处理中、有多少在网关排队等待。

- 适用场景：调用方在发送大请求前探测模型繁忙程度；运维脚本巡检；面板统计卡片自动展示（设了最大并发的模型显示"负载 x/n · 排队 m"）。
- 语义：`max_concurrency` 是**同时在上游处理中的请求数上限**（in-flight 并发），不是发送速率。第 X+1 个请求会在网关**挂起排队**等槽位（不拒绝、不跳模型）；发送速率另由 RPM/TPM 滑窗控制（触顶直接拒绝）。
- 占槽范围：非流式 = 从发出请求到上游返回响应；流式 = 从建连到流结束**全程占槽**（v2.12.0 起）；embedding / rerank 同非流式。

## 字段定义（三个端点返回结构一致）

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `model_id` | string | 回显请求的模型 ID |
| `max_concurrency` | int | 该模型配置的最大并发数；`0` = 不限 |
| `unlimited` | bool | `max_concurrency <= 0` 时为 `true`（不设上限，但仍统计实际负载） |
| `active` | int | 当前**持有并发槽、正在上游处理中**的请求数 |
| `waiting` | int | 当前**排队等待槽位**的请求数（含正要尝试获取槽的瞬时值，±1 抖动属正常） |

读法示例：`max_concurrency=5, active=5, waiting=3` → 已有 5 个请求在上游处理中、3 个在网关排队，此刻新请求会继续排队，直到有槽位腾出。

---

## 1️⃣ 管理端查询（管理密码）

```http
GET /admin/model/{model_id}/load
Authorization: Bearer <管理密码>
```

**curl 示例**

```bash
curl -H "Authorization: Bearer <管理密码>" \
     "http://127.0.0.1:8000/admin/model/HuoSan-Ark/glm-5.2/load"
```

**返回 `200`**

```json
{
  "model_id": "HuoSan-Ark/glm-5.2",
  "max_concurrency": 5,
  "unlimited": false,
  "active": 3,
  "waiting": 1
}
```

**错误**

| 状态码 | 场景 |
|:---|:---|
| `401` | 管理密码缺失/错误 |
| `404` | 模型 ID 不存在 |

---

## 2️⃣ 调用方查询（用户 API Key）

```http
GET /v1/model/load?model=<模型ID>
Authorization: Bearer <用户 API Key>
```

**curl 示例**

```bash
curl -H "Authorization: Bearer sk-xxxx" \
     "http://127.0.0.1:8000/v1/model/load?model=HuoSan-Ark/glm-5.2"
```

**返回 `200`**：与管理端完全相同的结构。

**错误**

| 状态码 | 场景 |
|:---|:---|
| `400` | 缺少 `model` 查询参数 |
| `401` | API Key 缺失/错误 |
| `404` | 模型 ID 不存在 |

> 该端点为只读负载信息，不做池权限过滤：任何有效 API Key 可查询任意模型 ID 的负载。

---

## 3️⃣ 面板批量查询（/stats 附带字段）

```http
GET /stats
Authorization: Bearer <管理密码>
```

响应的 `models[]` 每个模型对象在 v2.12.0 起额外携带：

```json
{
  "id": "HuoSan-Ark/glm-5.2",
  "max_concurrency": 5,
  "active_requests": 3,
  "waiting_requests": 1,
  "...": "其余既有字段不变"
}
```

hfadmin / admin 面板的用量统计卡片即消费此字段：`max_concurrency > 0` 的模型在卡片底部显示 `负载 3/5 · 排队 1`（排队为 0 时只显示 `负载 3/5`），随 5 秒轮询自动刷新；未设并发上限的模型不显示该小字。

---

## 行为细节与注意事项

1. **排队而非拒绝**：并发触顶时新请求在网关内排队等待槽位（客户端自身超时是兜底），不会因此收到 503；503 只来自配额/安全阀/RPM·TPM/全池不可用。
2. **热加载归零**：`POST /admin/reload` 或编辑模型后，运行时计数随旧模型实例丢弃，查询走新实例读到 0——在途请求仍由旧实例正常完成计费，不影响正确性。
3. **瞬时读数**：active/waiting 为采样瞬时值，高并发下相邻两次查询 ±1 抖动属正常。
4. **unlimited 模型**：`max_concurrency=0` 时不做并发 gating，但 active/waiting 照常统计，可用于观察真实负载后再决定是否设上限。
5. **与安全阀/RPM 的关系**：负载查询只反映并发与排队；配额（含安全阀 k）、RPM/TPM 触顶在预检阶段就已拒绝，不会出现在这些计数里。
